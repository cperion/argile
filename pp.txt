Here's the prompt and template:

---

## The Prompt

```
You have the complete Terra documentation in your context. I want you to produce a single, 
comprehensive, authoritative document about Terra that covers everything: philosophy, 
architecture, complete API, advanced metaprogramming patterns, performance techniques, 
DSL construction, language extension via the lexer/parser hooks, and real-world idioms.

This document should be written for an expert C/C++ programmer who is encountering Terra 
for the first time. It should not talk down to them, but it should not assume they know 
anything about Terra, LuaJIT internals, or the specific quirks of the system.

The document should be opinionated. Where Terra has multiple ways to do something, explain 
which way is idiomatic and why. Where Terra has sharp edges or surprising behavior, document 
them explicitly. Do not sanitize the rough parts — they are as important as the clean parts.

Use the document template below exactly. For each section marked [EXPAND], write the fullest, 
most accurate, most useful content you can produce based on the documentation in your context. 
Do not skip subsections. Do not summarize where you can be specific. Where the documentation 
gives API signatures, reproduce them completely and correctly. Where behavior is subtle or 
surprising, use callout blocks marked > ⚠️ WARNING or > 💡 NOTE.

Produce the complete document in one pass. Do not stop and ask for confirmation. 
Do not say "I'll cover X next." Just write the whole thing.

Length target: as long as it needs to be to be genuinely complete. 
Do not pad. Do not truncate. Depth over brevity.
```

---

## The Document Template

````markdown
# Terra: The Complete Reference

> *Terra is a low-level systems programming language designed to be meta-programmed 
> by LuaJIT. This document covers everything from first principles to production 
> patterns. It is intended to be the only Terra reference you need.*

**Version**: [Terra version from docs]  
**LuaJIT requirement**: [version from docs]  
**LLVM backend**: [version from docs]

---

## Table of Contents

1. [Philosophy and Design](#1-philosophy-and-design)
2. [Architecture: The Two-Phase Model](#2-architecture-the-two-phase-model)
3. [Installation and Toolchain](#3-installation-and-toolchain)
4. [The Terra/Lua Boundary](#4-the-terralua-boundary)
5. [Types](#5-types)
6. [Variables and Scoping](#6-variables-and-scoping)
7. [Expressions and Operators](#7-expressions-and-operators)
8. [Control Flow](#8-control-flow)
9. [Functions](#9-functions)
10. [Structs](#10-structs)
11. [Pointers and Memory](#11-pointers-and-memory)
12. [Arrays and Vectors](#12-arrays-and-vectors)
13. [Quotes and Escapes: The Metaprogramming Primitives](#13-quotes-and-escapes-the-metaprogramming-primitives)
14. [Macros](#14-macros)
15. [Exotypes: Operator Overloading and Metamethods](#15-exotypes-operator-overloading-and-metamethods)
16. [Environments and Symbol Resolution](#16-environments-and-symbol-resolution)
17. [The terralib API](#17-the-terralib-api)
18. [C Interop](#18-c-interop)
19. [Compilation Model and Targets](#19-compilation-model-and-targets)
20. [SIMD and Vector Programming](#20-simd-and-vector-programming)
21. [Atomic Operations and Memory Ordering](#21-atomic-operations-and-memory-ordering)
22. [Language Extensions: The Lexer/Parser Hook System](#22-language-extensions-the-lexerparser-hook-system)
23. [Building DSLs](#23-building-dsls)
24. [High-Performance Patterns via Metaprogramming](#24-high-performance-patterns-via-metaprogramming)
25. [Error Handling Patterns](#25-error-handling-patterns)
26. [Debugging and Introspection](#26-debugging-and-introspection)
27. [Complete API Reference](#27-complete-api-reference)
28. [Known Quirks and Sharp Edges](#28-known-quirks-and-sharp-edges)
29. [Patterns and Idioms](#29-patterns-and-idioms)
30. [The GIS Case Study: Everything Together](#30-the-gis-case-study-everything-together)

---

## 1. Philosophy and Design

### 1.1 Why Terra Exists
[EXPAND: The specific problems with C metaprogramming, C++ templates, Zig comptime. 
What gap Terra fills. The key insight: separate the meta-language from the object language 
cleanly, and make the meta-language a real programming language.]

### 1.2 The Deep Module Insight
[EXPAND: Languages as the ultimate deep module. Interface = text, implementation = LLVM. 
How this changes library design from "what functions do I expose" to "what should call 
sites look like".]

### 1.3 Design Decisions and Their Consequences
[EXPAND: Why LuaJIT specifically. Why LLVM. Why C compatibility. Why not GC in Terra. 
The explicit tradeoffs made and why they were the right ones for Terra's goals.]

### 1.4 What Terra Is Not
[EXPAND: Not a replacement for Lua. Not a safer C. Not a scripting language. 
Not a general-purpose high-level language. The specific role it plays.]

---

## 2. Architecture: The Two-Phase Model

### 2.1 Phase 1: Lua Execution
[EXPAND: What happens during the Lua phase. Building type definitions. Generating 
code programmatically. Specializing functions. Lua has full access to everything 
before Terra compiles anything.]

### 2.2 Phase 2: Terra Compilation
[EXPAND: What Terra receives from the Lua phase. Why compilation is fast — 
it sees only concrete, monomorphic, fully-resolved ASTs. The LLVM IR generation step. 
The difference from C++ template instantiation.]

### 2.3 JIT vs AOT
[EXPAND: Terra functions compiled on demand. When JIT compilation happens. 
How to force compilation with :compile(). Saving to .o and .a files for AOT. 
The production deployment model.]

### 2.4 The Symbol Table
[EXPAND: How Terra resolves names. The relationship between Lua variables and 
Terra symbols. Lexical scoping rules. The environment table and how it's used 
in language extensions.]

---

## 3. Installation and Toolchain

### 3.1 Building from Source
[EXPAND: Complete build instructions from docs. Dependencies. CMake invocation. 
Common build failures and their fixes.]

### 3.2 The terra Executable
[EXPAND: All command-line flags. What terra myfile.t does. The REPL. 
Running Lua files vs Terra files. The -e flag.]

### 3.3 Using Terra as a Library
[EXPAND: terra_State. Embedding in C applications. The C API surface. 
Initialization and shutdown. Thread safety constraints.]

### 3.4 Editor Support and LSP
[EXPAND: Available editor integrations. What the LSP does and doesn't support. 
How to get useful completions given the dynamic nature of the system.]

---

## 4. The Terra/Lua Boundary

### 4.1 Calling Terra from Lua
[EXPAND: Terra functions are callable as Lua functions. Type coercion at the boundary. 
What types cross cleanly and what requires care. The implicit conversion rules.]

### 4.2 Calling Lua from Terra
[EXPAND: This is NOT possible at runtime. Why. What you do instead — 
pass callbacks as function pointers, precompute in Lua and pass results as Terra values. 
This is a critical sharp edge.]

### 4.3 Passing Values Across the Boundary
[EXPAND: Primitive types. Structs as return values. Pointers. Strings — rawstring vs 
Lua strings. Arrays. What gets copied vs what gets referenced.]

### 4.4 Terra Functions as First-Class Lua Values
[EXPAND: terra functions are Lua tables with special metamethods. 
You can store them, pass them, index them. The :compile(), :disas(), :printpretty() 
methods. Using terra functions as C callbacks.]

---

## 5. Types

### 5.1 Primitive Types
[EXPAND: Complete list. int8 through int64, uint variants. float, double. 
bool. rawstring. opaque. niltype. Exact sizes guaranteed. No platform variation.]

### 5.2 The Type Hierarchy as Lua Values
[EXPAND: Types are Lua values. int32 is a Lua object. You can store types in 
variables, pass them to functions, return them. This is fundamental to metaprogramming.]

### 5.3 Pointer Types
[EXPAND: &T syntax. Pointer arithmetic. *p dereference. The difference between 
&T and &&T. Nullable pointers. How to express optional pointers.]

### 5.4 Array Types
[EXPAND: T[N] for fixed-size arrays. Zero-indexed. Stored inline (not as pointer). 
Passed by value (full copy). The surprising copy semantics vs C.]

### 5.5 Vector Types
[EXPAND: vector(T, N). Maps to LLVM vector types. SIMD operations. 
Which operations are vectorized. How to choose N for your target architecture.]

### 5.6 Function Types
[EXPAND: {T1,T2,...} -> {R1,R2,...} syntax. Multiple return values. 
Void functions. Function pointers. The difference between terra function values 
and C function pointers.]

### 5.7 Struct Types
[EXPAND: Forward reference to section 10. The struct keyword. Anonymous structs. 
Packed structs. The memory layout guarantees.]

### 5.8 Type Introspection
[EXPAND: terralib.typeof(). T:ispointer(), T:isarray(), T:isstruct(), etc. 
The full introspection API. Using introspection to write generic code in Lua.]

---

## 6. Variables and Scoping

### 6.1 var Declarations
[EXPAND: var x : T. var x = expr (type inferred). var x : T = expr. 
Multiple assignment: var x, y = f(). Uninitialized variables and their 
actual (undefined) initial values.]

### 6.2 Scoping Rules
[EXPAND: Block scoping with do...end. Loop variable scoping. The relationship 
between Terra scoping and Lua scoping. Where Lua variables are visible in 
Terra code via escapes.]

### 6.3 Global Variables
[EXPAND: global() declaration. Initialization semantics. Mutable globals. 
Constant globals. The difference from C's static storage.]

### 6.4 Constants
[EXPAND: Lua variables used as constants in Terra escapes. The [expr] escape 
bakes the value at compile time. Why Terra has no separate constant system 
— Lua variables serve this role.]

---

## 7. Expressions and Operators

### 7.1 Arithmetic and Logical Operators
[EXPAND: Complete operator table. Integer vs float semantics. 
Overflow behavior (wrapping, like C). Logical operators on booleans. 
Bitwise operators.]

### 7.2 Comparison Operators
[EXPAND: == ~= < > <= >=. Pointer comparison. Struct comparison (not supported 
by default — why, and how exotypes handle it).]

### 7.3 The @ Dereference and & Address-Of
[EXPAND: @ptr dereferences. &var takes address. The asymmetry: @ is an operator, 
& is part of type syntax AND an expression operator. Careful explanation of 
both uses of &.]

### 7.4 Casts
[EXPAND: [&T](expr), [T](expr). Explicit cast syntax. What casts are allowed. 
Truncating casts. Pointer-to-integer and integer-to-pointer. 
No implicit casts — this is intentional and important.]

### 7.5 Sizeof
[EXPAND: sizeof(T) and sizeof(expr). Returns a compile-time constant. 
Using sizeof in metaprogramming to make allocation generic.]

---

## 8. Control Flow

### 8.1 if / elseif / else / end
[EXPAND: Syntax. The condition must be bool or integer. No implicit conversion 
of pointers to bool — this is different from C and catches bugs. Nested if.]

### 8.2 Loops: while and repeat/until
[EXPAND: while condition do...end. repeat...until condition. The condition is 
evaluated after the body in repeat. break statement.]

### 8.3 Loops: for (numeric)
[EXPAND: for i = start, limit, step do...end. Inclusive limit. Integer and float 
variants. The step defaults to 1. The loop variable is local to the loop.]

### 8.4 Loops: for (iterator)
[EXPAND: If Terra supports iterator-style for — document it. If not, explain 
the pattern for building iterator-like constructs via Lua metaprogramming.]

### 8.5 goto and Labels
[EXPAND: goto label. ::label:: syntax. Valid uses. Why goto exists in Terra 
(C interop, generated code, state machines). The restrictions on jumping 
over variable declarations.]

### 8.6 defer
[EXPAND: defer statement. LIFO execution order. Interaction with return and goto. 
How to use defer for RAII. The critical edge cases — what happens if you 
defer inside a loop.]

### 8.7 return
[EXPAND: Single return value. Multiple return values. Returning structs by value 
(ABI implications). Early return patterns.]

---

## 9. Functions

### 9.1 terra Function Declaration
[EXPAND: terra function name(params) : returntype ... end. 
Anonymous terra functions. terra as an expression. The function value as Lua object.]

### 9.2 Function Overloading
[EXPAND: terralib.overloadedfunction(). How dispatch works. The resolution order. 
Limitations vs C++ overloading. When to use overloading vs generics via Lua.]

### 9.3 Multiple Return Values
[EXPAND: Declaring multiple return types. Capturing with var a, b = f(). 
What happens when you call a multi-return function and don't capture all values. 
Passing multi-return directly to another function call.]

### 9.4 Varargs
[EXPAND: The ... syntax. How varargs work in Terra. Interop with C variadic 
functions like printf. The type implications.]

### 9.5 Methods and Self
[EXPAND: terra MyStruct:method(params). The implicit self parameter. 
Method dispatch — it's static, not virtual. How to implement virtual dispatch 
via function pointers in structs (vtable pattern).]

### 9.6 Inline Functions and Optimization Hints
[EXPAND: How to hint to LLVM for inlining. terralib.intrinsic(). 
The alwaysinline attribute. When it matters and when LLVM inlines regardless.]

### 9.7 Recursion
[EXPAND: Forward declarations for mutual recursion. The pattern for declaring 
before defining. Why this is needed given the two-phase compilation model.]

---

## 10. Structs

### 10.1 Basic Struct Declaration
[EXPAND: local struct Name { field : Type; ... }. Semicolons vs newlines as 
separators. Anonymous structs. Forward declarations.]

### 10.2 Memory Layout
[EXPAND: Packed layout guarantees. Field ordering (same as declared). 
Alignment — Terra's rules. How to verify layout with offsetof. 
Comparison with C struct layout.]

### 10.3 Nested Structs
[EXPAND: Structs containing structs. By value (inline) vs by pointer. 
Anonymous nested structs for union-like patterns.]

### 10.4 Struct Methods
[EXPAND: Defining methods on structs. The self parameter. Static vs instance 
methods. The complete method syntax. Method tables in Lua.]

### 10.5 Constructors and Destructors via Metamethods
[EXPAND: __init and __destruct metamethods. When they're called. 
Interaction with defer. How to implement RAII. The gotchas with struct copying.]

### 10.6 Struct Introspection
[EXPAND: mystruct.entries — the list of fields as Lua data. 
Iterating fields at compile time. Using introspection to write generic serialization, 
printing, or transformation code.]

### 10.7 Opaque Structs and Forward Declarations
[EXPAND: struct Name; forward declaration. Pointer to incomplete type. 
How to work with C APIs that use opaque handles.]

---

## 11. Pointers and Memory

### 11.1 Stack Allocation
[EXPAND: var x : T — stack allocated. Lifetime tied to scope. 
When to use stack vs heap. The terra stack is the real CPU stack — 
no GC, no escape analysis.]

### 11.2 Heap Allocation
[EXPAND: No built-in new/delete. Use C.malloc/C.free via terralib.includec. 
The standard pattern. Arena allocators as the idiomatic alternative. 
Why arenas fit Terra's model well.]

### 11.3 Pointer Arithmetic
[EXPAND: ptr + n advances by sizeof(*ptr). ptr - ptr gives ptrdiff. 
The rules. Alignment implications. When pointer arithmetic is and isn't safe.]

### 11.4 Raw Memory Operations
[EXPAND: C.memcpy, C.memset, C.memmove via includec. 
terralib.cast for reinterpreting memory. The unsafe patterns that Terra allows 
and why they're explicit.]

### 11.5 Memory Safety Patterns
[EXPAND: How to build safe slices. Bounds checking in debug builds via 
Lua compile-time flags. The SafeSlice pattern. Ownership tracking via exotypes.]

---

## 12. Arrays and Vectors

### 12.1 Fixed-Size Arrays
[EXPAND: T[N]. Stack allocated. Full copy semantics. Indexing with [i]. 
The & trick to get a pointer into an array. Initializer syntax.]

### 12.2 Array Idioms
[EXPAND: Passing arrays — always pass by pointer. Returning arrays — 
return a struct containing the array. Multidimensional arrays. 
Row-major layout and cache implications.]

### 12.3 SIMD Vectors
[EXPAND: vector(float, 4) etc. Arithmetic operations vectorize automatically. 
Inserting and extracting elements. Loading and storing from memory. 
Shuffle operations. The relationship to x86 SSE/AVX intrinsics.]

### 12.4 Vector Programming Patterns
[EXPAND: AoS to SoA transformation and why it matters for SIMD. 
Processing multiple elements simultaneously. Loop vectorization vs explicit vectors. 
Measuring vectorization via disassembly.]

---

## 13. Quotes and Escapes: The Metaprogramming Primitives

### 13.1 The Quote Operator `
[EXPAND: `expr creates a quoted expression — an AST node as a Lua value. 
`{ statements } creates a quoted statement list. The type of a quote. 
Quotes are not evaluated — they represent code that will be compiled later.]

### 13.2 The Escape Operator []
[EXPAND: [luaexpr] inside a Terra context evaluates luaexpr in Lua and 
splices the result into the Terra AST. The result must be a Terra value, type, 
or quote. How splicing works. The complete rules for what can be spliced.]

### 13.3 Splicing Types
[EXPAND: [T] where T is a Lua variable holding a Terra type. 
var x : [mytype]. The pattern for generic types via escape.]

### 13.4 Splicing Values
[EXPAND: [value] where value is a Lua number, string, or Terra primitive. 
The value is baked into the compiled code as a literal. 
How this differs from a runtime variable.]

### 13.5 Splicing Quotes
[EXPAND: [quotedExpr] splices a quoted expression into the AST. 
The fundamental pattern for code generation. How to build complex expressions 
programmatically and splice them.]

### 13.6 Splicing Statement Lists
[EXPAND: [list] where list is a list of quoted statements. 
Unquote-splicing. Building loops, conditionals, and sequences programmatically.]

### 13.7 The exprlist Pattern
[EXPAND: How to build argument lists dynamically. Passing computed argument 
lists to function calls. The pattern for variadic-by-metaprogramming functions.]

---

## 14. Macros

### 14.1 What Terra Macros Are
[EXPAND: macro(function(ctx, ...) ... end). A Lua function that receives 
a compilation context and returns a quoted expression. The key difference from 
C macros: hygiene, type information, full Lua power.]

### 14.2 Macro Parameters
[EXPAND: The ctx parameter. The argument parameters — they are typed AST nodes, 
not values. The introspection API on macro arguments: :gettype(), :asvalue(), etc. 
How to inspect what was passed to the macro.]

### 14.3 Hygiene
[EXPAND: How Terra macros avoid variable capture. Generating fresh symbols 
with symbol(). The pattern for hygienic temporaries. When hygiene matters.]

### 14.4 Macros vs Functions
[EXPAND: When to use a macro vs a regular Terra function. Macros can inspect 
argument types and generate different code based on them. Macros can create new 
scopes. The overhead: macros run at compile time, functions at runtime.]

### 14.5 Macro Patterns
[EXPAND: The assert macro pattern. The defer-like macro. 
The min/max macro without double-evaluation. Generic swap. 
Type-dispatching macros that generate different code for float vs int.]

---

## 15. Exotypes: Operator Overloading and Metamethods

### 15.1 What Exotypes Are
[EXPAND: struct with Lua metamethods instead of (or in addition to) Terra methods. 
The metamethod table. How Terra calls metamethods. The complete list of available 
metamethods.]

### 15.2 Arithmetic Metamethods
[EXPAND: __add, __sub, __mul, __div, __mod, __unm. 
The signature each must have. Return type rules. How LLVM sees the resulting code 
(no overhead — just Terra function calls that get inlined).]

### 15.3 Comparison Metamethods
[EXPAND: __eq, __lt, __le. How to implement partial order vs total order. 
The limitations — can't override ~= directly.]

### 15.4 Index and Newindex
[EXPAND: __index for element access. __newindex for element assignment. 
The signature. How to implement safe array access with bounds checking at 
compile time vs runtime.]

### 15.5 __apply: Making Structs Callable
[EXPAND: __apply makes a struct usable as a function. The pattern for callable 
types. Use cases: functors, closures, lazy evaluation.]

### 15.6 __cast: Implicit Conversion
[EXPAND: __cast for implicit type conversion. When it fires. The signature. 
How to implement Vec2 that accepts {x,y} struct literals. 
The risk of overusing __cast.]

### 15.7 __init and __destruct: RAII
[EXPAND: __init called after construction. __destruct called at scope exit 
(with defer integration). The RAII pattern in full. Interaction with copy 
and assignment. The sharp edges: __destruct on function arguments.]

### 15.8 __getmethod: Dynamic Method Dispatch
[EXPAND: __getmethod(self, methodname) called when a method isn't found. 
Returning a terra function or macro. The power: runtime-determined methods, 
ownership tracking, proxy types. This is one of Terra's most powerful features.]

---

## 16. Environments and Symbol Resolution

### 16.1 Default Symbol Resolution
[EXPAND: How Terra looks up names. First checks Terra locals. Then checks 
the environment table. The default environment is the Lua global table. 
C symbols available via includec are in the environment.]

### 16.2 Custom Environments
[EXPAND: Passing a custom env table to terralib.loadstring or defining 
functions inside a custom environment. How this is used to restrict or extend 
the available symbols. The pattern for sandboxed DSLs.]

### 16.3 Environment Composition
[EXPAND: Merging environments. Inheriting from parent environments. 
The pattern for layered symbol tables. How to give DSL code access to 
some globals but not others.]

### 16.4 Environments for Safety
[EXPAND: Using environments to block unsafe operations: removing C.malloc, 
blocking terralib.cast, requiring all allocations to go through a safe API. 
The memory safety extension pattern.]

---

## 17. The terralib API

### 17.1 Core Compilation Functions
[EXPAND: terralib.loadstring(), terralib.loadfile(), terralib.require(). 
Return values. Error handling. The chunk function pattern.]

### 17.2 Type Constructors
[EXPAND: terralib.types.pointer(T), terralib.types.array(T,N), 
terralib.types.vector(T,N), terralib.types.struct(). 
Building types programmatically from Lua.]

### 17.3 Function Manipulation
[EXPAND: myfn:compile(), myfn:disas(), myfn:printpretty(), myfn:gettype(). 
myfn:getdefinitions() for overloaded functions. 
Forcing compilation. Inspecting generated IR.]

### 17.4 Saving Compiled Code
[EXPAND: terralib.saveobj(filename, filetype, fns, optimize). 
File types: "object", "bitcode", "llvmir", "executable", "sharedlibrary". 
The optimize parameter. Generating .o files for linking. Generating LLVM bitcode 
for emscripten/WASM pipeline.]

### 17.5 Linking
[EXPAND: terralib.linklibrary(). Linking .so and .dylib files. 
Finding symbols. The search path. Linking at JIT time vs AOT.]

### 17.6 Symbol and Quote Utilities
[EXPAND: symbol(type, name). terralib.issymbol(). 
terralib.isquote(). terralib.istype(). terralib.isfunction(). 
terralib.isglobalvar(). The complete set of type-testing predicates.]

### 17.7 Intrinsics
[EXPAND: terralib.intrinsic(name, type). Calling LLVM intrinsics directly. 
Common intrinsics: sqrt, fma, prefetch, memcpy. The complete pattern. 
How to find the LLVM intrinsic name for a given operation.]

### 17.8 Atomic Operations
[EXPAND: terralib.atomicrmw(op, ptr, val, ordering). 
terralib.cmpxchg(ptr, cmp, val, ordering). 
The complete operation list. Memory ordering semantics (relaxed/acquire/release/acq_rel/seq_cst). 
Mapping to hardware instructions.]

### 17.9 Targets
[EXPAND: terralib.newtarget({Triple=..., CPU=..., Features=...}). 
Cross-compilation. WASM target triple. Available CPU names and feature strings. 
How to query the host target. Compiling for multiple targets from one process.]

---

## 18. C Interop

### 18.1 terralib.includec()
[EXPAND: How it works — calls Clang, parses the header, returns a Lua table. 
The table structure: functions, types, macros, constants. 
Search path configuration. System headers. Preprocessor defines.]

### 18.2 What Gets Imported
[EXPAND: Functions: available as Terra functions. 
Structs: available as Terra struct types. 
Typedefs: resolved. Enums: available as integer constants. 
Macros: simple constant macros become Lua numbers; function macros do NOT import.]

### 18.3 Function Macros and Inline Functions
[EXPAND: C function macros can't be imported — they don't exist in the AST. 
C inline functions also may not be available. The workaround patterns: 
write a thin C wrapper, or reimplement in Terra.]

### 18.4 ABI Compatibility
[EXPAND: Terra structs are C-compatible. Terra functions use C calling convention 
by default. Struct layout matches C. Passing Terra structs to C functions and vice versa. 
The cases where ABI breaks (va_args, bitfields).]

### 18.5 The includecstring Pattern
[EXPAND: terralib.includecstring("C source code"). 
Useful for: defining simple inline wrappers, creating constants from C macros 
that can't be imported, working around inline function limitations.]

### 18.6 Linking C Libraries
[EXPAND: After includec, you still need to link. The pattern: includec for types 
and declarations, linklibrary for the actual symbols. pkg-config integration pattern. 
Static vs dynamic linking implications.]

---

## 19. Compilation Model and Targets

### 19.1 JIT Compilation
[EXPAND: Terra functions are JIT-compiled on first call. The compilation 
happens once per function, result cached. Forcing compilation with :compile(). 
Thread safety of JIT compilation.]

### 19.2 AOT Compilation
[EXPAND: saveobj for ahead-of-time compilation. The workflow: write Terra, 
run a build script that saves .o files, link with the rest of your project. 
No Terra runtime needed in production. The generated code is plain native code.]

### 19.3 LLVM IR
[EXPAND: saveobj with "llvmir" type. Reading the IR for debugging. 
How to pipe to opt for manual optimization passes. The bitcode format for 
WASM/emscripten pipeline.]

### 19.4 Cross Compilation
[EXPAND: newtarget with explicit triple. The limitations — includec still 
uses host headers. The practical patterns for cross-compilation. 
Generating WASM via bitcode → emscripten.]

### 19.5 Optimization Levels
[EXPAND: The optimize parameter to saveobj. What each level does. 
The interaction between Terra-level optimization and LLVM-level optimization. 
When to use which level.]

---

## 20. SIMD and Vector Programming

### 20.1 SIMD Via vector() Types
[EXPAND: Complete coverage of the vector type and its operations. 
What operations are available. What generates actual SIMD vs scalar fallback. 
How to verify in disassembly.]

### 20.2 LLVM Intrinsics for SIMD
[EXPAND: Using terralib.intrinsic to access platform-specific SIMD. 
x86 SSE/AVX intrinsics. ARM NEON. The portability tradeoff. 
When to prefer vector() over intrinsics.]

### 20.3 Data Layout for SIMD
[EXPAND: Array of Structs vs Struct of Arrays. Why SoA is usually faster with SIMD. 
The transformation patterns. Practical examples with performance implications.]

### 20.4 Auto-vectorization via LLVM
[EXPAND: Writing scalar loops that LLVM vectorizes automatically. 
The conditions LLVM requires. How to check if vectorization happened. 
Hints that help LLVM vectorize: no aliasing, simple control flow, aligned loads.]

---

## 21. Atomic Operations and Memory Ordering

### 21.1 The Atomic API
[EXPAND: Complete coverage of terralib.atomicrmw and terralib.cmpxchg. 
All available operations. The ptr must be a pointer to integer or pointer type. 
Return value semantics.]

### 21.2 Memory Ordering Semantics
[EXPAND: What each ordering means in practice. When to use relaxed (counters 
with no synchronization requirement). When to use acquire/release (lock implementation). 
When to use seq_cst (when in doubt). The performance implications.]

### 21.3 Building Synchronization Primitives
[EXPAND: Spinlock from atomicrmw("xchg"). Seqlock pattern. 
Atomic reference counting. Wait-free stack. The complete patterns with explanation.]

### 21.4 Integration with pthreads
[EXPAND: Terra functions as pthread_create callbacks. 
The Lua-is-not-thread-safe constraint and what it means in practice. 
Compile all Terra before spawning threads. The safe patterns.]

---

## 22. Language Extensions: The Lexer/Parser Hook System

### 22.1 How the Extension System Works
[EXPAND: The entrypoints and keywords tables. What an entrypoint is — 
a keyword that starts a custom expression or statement. The lexer object. 
How Terra hands control to your parser.]

### 22.2 The Lexer API
[EXPAND: Complete lexer API. lexer:next() returns the next token with 
.type and .value. lexer:peek() looks ahead without consuming. 
lexer:expect(tok) consumes or errors. lexer:matches(tok) tests without consuming. 
lexer:terraexpr() parses a full Terra expression and returns a quote. 
lexer:terrastats() parses a statement list. The token types.]

### 22.3 Writing an Expression Extension
[EXPAND: The expression form: entrypoints list, the expression function 
receives self and lexer, returns a function from environment to quote. 
The full pattern with working example.]

### 22.4 Writing a Statement Extension
[EXPAND: The statement form: similar but the statement function. 
When to use expression vs statement extension. Block statements that contain 
bodies parsed with the lexer.]

### 22.5 Registering Extensions
[EXPAND: Passing extension objects to terralib.loadstring, terralib.loadfile. 
How to make extensions available globally. The terralib.require integration. 
How extensions compose — using multiple extensions simultaneously.]

### 22.6 Recursive Extensions
[EXPAND: An extension that calls itself — the pattern for nested block syntax. 
The thing(params) children end pattern. How to parse children using the 
extension's own statement parser recursively.]

### 22.7 Extension Error Handling
[EXPAND: Signaling parse errors with correct line numbers. 
The error API on the lexer. How errors propagate. Producing useful error messages 
that point to user code rather than extension internals.]

---

## 23. Building DSLs

### 23.1 The Philosophy: Notation Over API
[EXPAND: Why you'd provide a DSL instead of an API. The deep module argument. 
When DSLs are worth the investment. When they're not.]

### 23.2 Parsing Strategies
[EXPAND: Token-by-token with the lexer API. Using terraexpr() to delegate 
expression parsing back to Terra. Mixed strategies. 
How to handle operator precedence in custom syntax.]

### 23.3 Building ASTs from the DSL
[EXPAND: The Lua data structures for representing your DSL's parse tree. 
Converting to Terra quotes. Generating Terra struct definitions from DSL specs. 
The pattern for DSL → Lua data → Terra code.]

### 23.4 Type Checking in DSLs
[EXPAND: How to add domain-specific type checking to your DSL. 
Using the information available in quotes to verify correctness. 
Emitting compile-time errors from Lua with the right source location.]

### 23.5 The thing(params) children end Pattern
[EXPAND: Complete implementation. The recursive parser. The node tree data structure. 
Generating Terra code from the tree. How nesting works. Multiple examples.]

### 23.6 DSL Examples
[EXPAND: State machine DSL with complete parser and code generator. 
Query DSL showing type-checked field access. 
UI layout DSL showing tree generation. 
Shader pipeline DSL showing cross-stage type checking.]

---

## 24. High-Performance Patterns via Metaprogramming

### 24.1 Specialization Over Generics
[EXPAND: The core pattern: Lua function returns specialized Terra type. 
Why specialization is better than C++ templates. Compilation speed. 
Zero cost abstraction — the specialization runs at meta-time, not runtime.]

### 24.2 Compile-Time Loop Unrolling
[EXPAND: Using Lua for loops to generate unrolled Terra code. 
Quote accumulation. When unrolling helps. When it hurts. 
How to parameterize the unroll factor.]

### 24.3 Baking Constants into Code
[EXPAND: The [constant] escape bakes values as literals. Why this is faster 
than loading from memory. Examples: lookup tables, algorithm parameters, 
stride values for array access. The interaction with LLVM constant folding.]

### 24.4 Struct Layout Optimization via Metaprogramming
[EXPAND: Computing optimal field ordering from access patterns. 
Generating struct definitions with optimal padding. Cache line alignment. 
Separating hot and cold fields.]

### 24.5 Generic Algorithms
[EXPAND: The Lua function(T) -> terra function pattern for generic algorithms. 
Comparison functions as compile-time parameters. 
Sort, search, and reduction algorithms that specialize per type. 
The dispatch table pattern.]

### 24.6 JIT Specialization at Runtime
[EXPAND: Calling Lua from the host program at runtime to generate and compile 
new Terra functions. The query specialization pattern. 
How fast compilation makes this practical. Examples: query planner, shader compiler, 
dynamic dispatch elimination.]

### 24.7 Profile-Guided Specialization
[EXPAND: Observing which code paths are hot, then using Lua to regenerate 
specialized Terra code. The closed-world assumption and why Terra can exploit it. 
The pattern for adaptive specialization.]

---

## 25. Error Handling Patterns

### 25.1 Error Handling Philosophy in Terra
[EXPAND: No exceptions. No built-in result type. The explicit patterns. 
Why this is right for system code. The available approaches.]

### 25.2 Error Codes
[EXPAND: Returning int error codes, C-style. Conventions. 
Multiple return values for (result, error) pairs. The pattern and its limitations.]

### 25.3 Result Types via Metaprogramming
[EXPAND: Building a Result(T, E) type with Lua. The OK/Err variants. 
Propagation patterns — the Terra equivalent of ?. 
When this is worth it vs simple error codes.]

### 25.4 Panic and Assertions
[EXPAND: C.abort() for unrecoverable errors. Build-time assertion generation. 
Debug vs release assertion stripping via Lua flags. 
The pattern for annotating unreachable code.]

### 25.5 Setjmp/Longjmp Interop
[EXPAND: If using setjmp/longjmp for error handling in C libraries — 
how Terra interacts with this. The ABI constraints. The dangers.]

---

## 26. Debugging and Introspection

### 26.1 printpretty: Reading Terra ASTs
[EXPAND: myfn:printpretty(). What it shows. How to read the output. 
Using it to verify that metaprogramming generated what you expected. 
The output format.]

### 26.2 disas: Reading Generated Assembly
[EXPAND: myfn:disas(). What it shows. How to verify SIMD vectorization happened. 
How to check that constants were baked in. 
How to verify inlining happened. Reading x86 assembly basics for Terra programmers.]

### 26.3 terralib.printversion()
[EXPAND: Version information. Diagnostics.]

### 26.4 Runtime Debugging
[EXPAND: Printf debugging (C.printf via includec). 
The escape valve: calling Lua from the build script to log information. 
GDB integration — Terra code is native, GDB works. 
The sourcemap — line number information in compiled code.]

### 26.5 Type and Quote Inspection
[EXPAND: terralib.typeof() on Terra values. quote introspection API. 
Inspecting the AST of a quoted expression. Using introspection in 
metaprogrammed code to adapt to the types it receives.]

---

## 27. Complete API Reference

> This section is a comprehensive, structured reference for every function and 
> value in the terralib namespace. It supplements rather than replaces the 
> preceding sections.

### 27.1 terralib Functions (alphabetical)
[EXPAND: Every function in terralib with full signature, parameter descriptions, 
return values, and a minimal example. No function should be omitted.]

### 27.2 Terra Type Methods
[EXPAND: Methods available on all Terra type objects: :isprimitive(), :ispointer(), 
:isarray(), :isvector(), :isstruct(), :isfunction(), :isintegral(), :isfloat(), 
:isarithmetic(), :isaggregate(), :iscomplete(). 
Type-specific methods. The full introspection surface.]

### 27.3 Terra Function Methods
[EXPAND: Methods on terra function values: :compile(), :disas(), :printpretty(), 
:gettype(), :getdefinitions(), :getpointer(). 
With signatures and examples.]

### 27.4 Terra Struct Methods
[EXPAND: Methods on struct type objects: .methods, .entries, .metamethods, 
:complete(), :setconvertible(), :getmethod(). 
The complete struct introspection surface.]

### 27.5 Quote Methods
[EXPAND: Methods available on quote values: :gettype(), :asvalue(), 
:islvalue(). Complete list with descriptions.]

### 27.6 Lexer API (for language extensions)
[EXPAND: Complete lexer method reference for use in parser extensions. 
Every method, its signature, what it consumes, what it returns.]

---

## 28. Known Quirks and Sharp Edges

> This section documents behaviors that are correct but surprising. 
> Read it before you spend hours debugging.

### 28.1 Lua Is Not Thread Safe
[EXPAND: Full explanation of the constraint. 
The consequences for library design. The correct pattern: 
compile all Terra in the main thread before spawning workers.]

### 28.2 No Calling Lua from Terra at Runtime
[EXPAND: This is a hard constraint. Why it exists (Lua GC, Lua stack). 
The patterns that look like they should work but don't. 
The correct alternatives: callbacks compiled before the call, 
state threaded through as Terra values.]

### 28.3 Terra Struct Copy Semantics
[EXPAND: Terra structs copy by value when assigned or passed. 
For large structs this is a performance hazard. The pattern: 
always pass structs by pointer. When copy semantics cause bugs: 
modifying a copy thinking you're modifying the original.]

### 28.4 The & Ambiguity
[EXPAND: & means both "address-of expression" and "pointer type constructor". 
The rules for which is which. The cases where it's ambiguous and how to resolve.]

### 28.5 Uninitialized Variables
[EXPAND: var x : int has no guaranteed initial value. This is intentional — 
zero initialization has a cost. The discipline required. 
Debug patterns for finding uninitialized reads.]

### 28.6 __destruct and Function Arguments
[EXPAND: The sharp edge: structs with __destruct that are passed by value 
to functions. When destruct fires. The pattern to avoid double-destruction. 
This is a common source of bugs when implementing RAII.]

### 28.7 Escape Evaluation Order
[EXPAND: [expr] escapes evaluate Lua at parse time, not at function call time. 
The consequences for code that relies on mutable Lua state. 
The pattern that looks like it should be a runtime decision but is actually 
compile-time.]

### 28.8 includec and Host Headers
[EXPAND: includec uses the host compiler's headers. Cross-compilation implications. 
Why Windows headers can cause problems. System-specific includes and portability.]

### 28.9 Macro Arguments Are Untyped Until Analyzed
[EXPAND: Macro arguments are AST nodes, not values. The common mistake: 
treating them like values before calling :gettype(). 
How to correctly inspect macro arguments.]

### 28.10 The LuaJIT 2GB Limit
[EXPAND: LuaJIT's memory allocator is limited to 2GB of virtual address space 
on some platforms. How this affects Terra programs that allocate large amounts 
of memory for their data (not their code). 
The pattern: allocate large buffers with C.malloc, keep Lua/Terra code small.]

---

## 29. Patterns and Idioms

### 29.1 The Generic Type Pattern
```lua
local function Vec(T)
  local struct VecT { ... }
  terra VecT:method() ... end
  return VecT
end
local Vec3f = Vec(float)
local Vec3d = Vec(double)
```
[EXPAND: Full explanation and variations.]

### 29.2 The Cached Specialization Pattern
```lua
local cache = {}
local function get_vec(T)
  if not cache[T] then cache[T] = make_vec(T) end
  return cache[T]
end
```
[EXPAND: Why caching matters. When to cache vs always regenerate.]

### 29.3 The Compile-Time Dispatch Pattern
[EXPAND: Using Lua if/else on type information to generate different Terra code 
for different types. The integer vs float dispatch example. 
How this eliminates runtime branching entirely.]

### 29.4 The Arena Allocator Pattern
[EXPAND: Complete arena implementation in Terra. 
Push/pop allocation. Reset without freeing. Why arenas fit Terra's model. 
Thread-local arenas.]

### 29.5 The Builder Pattern
[EXPAND: Lua object that accumulates information, then call :compile() or :build() 
to generate a Terra type or function. DSL construction via builder. 
When builders are cleaner than direct code generation.]

### 29.6 The Vtable Pattern
[EXPAND: Implementing runtime polymorphism in Terra via function pointer structs. 
Interface types as structs of function pointers. Generating vtables via Lua metaprogramming. 
Performance characteristics vs C++ vtables.]

### 29.7 The Module Pattern
[EXPAND: Structuring a Terra library as a Lua module. What to put in the module table. 
How to expose Terra types, Terra functions, and Lua utilities in one coherent module. 
The require integration.]

### 29.8 The Fibers Pattern
[EXPAND: Cooperative multitasking via ucontext. Scheduler implementation. 
Typed channels. The goroutine-style go keyword extension. Full pattern.]

---

## 30. The GIS Case Study: Everything Together

> This section demonstrates every major concept from this document applied to 
> a realistic production library: a high-performance spatial data processing 
> library with multi-language bindings.

### 30.1 Architecture Overview
[EXPAND: How the library is structured. What's in Terra vs Lua. 
The DSL interfaces. The binding generation strategy.]

### 30.2 Geometry Types with CRS Phantom Types
[EXPAND: Using phantom types to prevent mixing coordinate systems. 
The CRS type as a compile-time marker. How to encode WGS84, WebMercator, 
UTM as distinct phantom types. The Point(CRS) pattern.]

### 30.3 The Spatial Index as Generic Terra
[EXPAND: R-tree implementation generic over geometry type. 
SIMD bounding box tests. The Lua(T) -> Terra pattern applied at scale. 
Serialization via introspection.]

### 30.4 The Query DSL
[EXPAND: Complete query DSL: from, where, select, order, limit with 
parser, type checker, and code generator. Field access type checking. 
Compile-time query plan generation.]

### 30.5 Auto-Generated Multi-Language Bindings
[EXPAND: The binding generator applied to the GIS library. 
Python ctypes output. LuaJIT FFI output. WASM via emscripten. 
The build system integration.]

### 30.6 Performance Numbers
[EXPAND: Realistic benchmarks vs equivalent implementations. 
What the metaprogramming actually buys in terms of generated code quality. 
Profiling approach.]

---

## Appendix A: Grammar Reference

[EXPAND: Formal grammar for Terra's syntax. BNF or EBNF. 
Statement forms, expression forms, type expressions, the quote/escape syntax. 
The lexer token types.]

## Appendix B: LLVM Intrinsics Quick Reference

[EXPAND: The most commonly used LLVM intrinsics accessible via terralib.intrinsic(). 
Names, types, and brief descriptions.]

## Appendix C: Platform Notes

[EXPAND: macOS specifics. Linux specifics. Windows status and limitations. 
WASM target details. ARM architecture notes.]

## Appendix D: Changelog and Version History

[EXPAND: What changed in recent versions. Breaking changes. 
Migration notes.]

---

*Generated from the complete Terra documentation.*
````

---

## Usage Notes

A few things to add when you send this:

**Tell it to run straight through**: "Do not stop between sections. Do not ask for confirmation. Do not say 'shall I continue.' Write the complete document."

**Tell it to be specific over general**: "Where the documentation gives exact API signatures, reproduce them exactly. Do not paraphrase APIs."

**Tell it to flag gaps**: "If a section's documentation is incomplete or absent from what you have, write what you know and mark the gap with > ⚠️ DOCUMENTATION GAP: [what's missing] so I can fill it manually."

**On the case study**: Section 30 draws on our conversation, not the Terra docs. You may want to write that section yourself or prompt it separately with this conversation as context.

The template is structured so each `[EXPAND]` block is independently completable. If the model gets cut off partway through, you can resume with "Continue from section X.Y" and it'll slot in cleanly.
